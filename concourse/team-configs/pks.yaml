roles:
- name: "owner"
  github:
    teams: [
      "pivotal-cf:pks-releng",
      "pivotal-cf:pks-core",
      "pivotal-cf:pks-bosh-lifecycle"
    ]
    users: ["jaimegag", "svrc-pivotal"]
- name: "viewer"
  github:
    teams: [
      "pivotal-cf:pks"
    ]

# To apply this file, run this command:
# fly -t kubo set-team -n pks -c pks.yaml
