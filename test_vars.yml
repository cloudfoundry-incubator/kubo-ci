kubo-version-bucket: &release-bucket kubo-pipeline-store-test
kubo-ci-branch: combine_windows
kubo-deployment:
  branch: &develop combine_windows
  build-bucket: *release-bucket
kubo-release:
  branch: merge_repos
  ubuntu-build-bucket: *release-bucket
  windows-build-bucket: *release-bucket
precompiled-release-bucket: kubo-precompiled-releases